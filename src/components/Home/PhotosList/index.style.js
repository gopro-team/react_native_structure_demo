import { objToStyle } from 'utils/style';
import { size } from 'styles/base.style';


export default objToStyle({
  list: {
    flexWrap: 'wrap',
    flexDirection: 'row',
    padding: size.xs,
    height: 'auto',
  },
});
